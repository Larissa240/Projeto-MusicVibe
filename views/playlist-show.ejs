<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8" />
  <title>Minha Playlist</title>
  <link rel="stylesheet" href="/stylesheets/style-playlist.css" />
</head>
<body>

<div class="player-container">
  <div class="banner-container">
    <img src="/images/coldplay.png" class="banner-img" alt="Coldplay">
    <button class="play-button">▶</button>
  </div>

  <div class="playlist-header">
  <h2><%= playlist.nome %></h2>
</div>

<ul class="music-list">
  <% if (musicas.length === 0) { %>
    <li>Nenhuma música nesta playlist.</li>
  <% } else { %>
    <% musicas.forEach(musica => { %>
      <li class="music-item">
        <img src="/images/default-album.png" class="album-cover" alt="Capa">
        <div class="music-info">
          <p class="titulo"><%= musica.titulo %></p>
          <p class="artista"><%= musica.artista || 'Artista Desconhecido' %></p>
        </div>
        <button class="like-button">🤍</button>
      </li>
    <% }) %>
  <% } %>
</ul>






